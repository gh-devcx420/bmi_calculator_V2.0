enum Gender {
  none,
  male,
  female,
}

enum HeightMetric {
  feetinches,
  cms,
}

enum WeightMetric {
  kgs,
  pounds,
}

enum ResultInference {
  none,
  underweight,
  normal,
  slightlyOverweight,
  overweight,
  unrealisticInputs,
}
